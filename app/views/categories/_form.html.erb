<h1>Create A Category</h1>

<% if category.errors %>
	<ul>
		<% category.errors.full_messages.each do |error| %>
		<li><%= error %></li>
		<% end %>
	</ul>

<% end %>

<%= form_for category do |f| %>
	<%= f.label :name %>
	<br>
	<%= f.text_field :name %>
	<br>
	<%= f.submit %>
<% end %>